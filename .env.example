# Copy this file to .env and fill in your real values
BOT_TOKEN=your_bot_token_here

# Local path — works on any machine / environment
# On Render (Docker) the value is overridden to /data/leinerfrog.db via render.yaml
DB_PATH=leinerfrog.db
